/* Core */
import { createAsyncThunk } from '@reduxjs/toolkit';

/**
 * A utility function to create Async Thunk Actions.
 * (Simplified for JavaScript — no TypeScript typing required)
 */
export const createAppAsyncThunk = createAsyncThunk;
